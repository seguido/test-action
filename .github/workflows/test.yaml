name: Test

on: 
  push:
    branches: 
      - 'test3/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./
        with:
          GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}
