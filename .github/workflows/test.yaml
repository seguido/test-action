name: Test

on: 
  push:
    branches: 
      - 'test3/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: seguido/beefy-app-sync@v0.1
        with:
          GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}
          GITHUB_USER: 'chebiN'
          GITHUB_EMAIL: '17909998+seguido@users.noreply.github.com'
